<%= will_paginate %>

<table id="users" class="table">
  <tr>
    <th><%= sort_link @q, :name, 'user name' %></th>
    <th class="text-center"><%= sort_link @q, :followers, 'followers' %></th>
    <th class="text-center"><%= sort_link @q, :state, 'active?' %></th>
    <th class="text-center">remove</th>
  </tr>
  <%= render @users %>
</table>

<%= will_paginate %>