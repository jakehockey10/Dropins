<li class="dropdown">
  <% if unread_count.to_i > 0 %>
      <a href="#" style="padding-top: 10px; padding-bottom: 10px;" class="dropdown-toggle" data-toggle="dropdown">
        <%= fa_icon 'bell 2x', class: 'new-notification', text: raw("<span class='badge'>#{unread_count}</span>") %>
      </a>
      <ul class="dropdown-menu notification-menu">
        <% mailbox.inbox.unread(current_user).each do |m| %>
            <li>
              <%= link_to render('conversations/menu_conversation', conversation: m), conversation_path(m) %>
            </li>
        <% end %>
      </ul>
  <% else %>
      <a href="#" style="padding-top: 10px; padding-bottom: 10px;" class="dropdown-toggle" data-toggle="dropdown">
          <%= fa_icon 'bell-o 2x' %>
      </a>
      <ul class="dropdown-menu notification-menu">
        <li><%= link_to 'There are no new notifications...', '#' %></li>
      </ul>
  <% end %>
</li>