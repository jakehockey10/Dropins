<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title)) %></title>
  <%= favicon_link_tag 'favicon.ico', rel: 'short icon' %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'jcarousel' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= render 'layouts/shim' %>
</head>
<body>
  <%= render 'layouts/header' %>
  <div class="container">
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "alert alert-#{key} alert-dismissable") do %>
        <%= value %>
        <button type="button" class="close">&times</button>
      <% end %>
    <% end %>
    <div class="container">
      <%= yield %>
    </div>

    <%= debug(params) if Rails.env.development? %>
    <%= debug("current_user is nil: #{current_user.nil?}") if Rails.env.development? %>

    <%= render 'layouts/footer' %>
  </div>
</body>
</html>
