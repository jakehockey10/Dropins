<% provide(:title, 'Dropins') %>

<div class="page-header">
  <h1>Dropins <small class="pull-right"></small></h1>
</div>

<div class="row">
  <aside class="col-sm-4 col-md-4">
    <section>
      <h1>Create a new dropin:</h1>
      <%= render 'dropins/form', dropin: Dropin.new %>
    </section>
    <section>

    </section>
  </aside>
  <div class="col-sm-8 col-md-8 dropins-table">
    <%= render 'dropins_table', dropins: @dropins %>
  </div>
</div>