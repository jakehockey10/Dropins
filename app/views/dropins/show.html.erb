<% provide(:title, 'Dropin') %>

<%= render 'pay_modal' %>

<h1 class="page-header">Dropin <%= @dropin.id %></h1>

<div class="row">
  <%= render 'dropin_aside' %>

  <div class="col-sm-8 col-md-8 dropin-skaters">
    <%# render 'attendances/form' if signed_in? %>

    <%= render 'skaters_table' %>
  </div>
</div>
